<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Blogify</title>
  <link rel="icon" type="image/png"
    href="https://media.licdn.com/dms/image/v2/D5603AQHCgvnGewxUxA/profile-displayphoto-shrink_400_400/B56Zch7E49G0Ak-/0/1748620825165?e=1755734400&v=beta&t=-P56pMvRfXbJYJuyld04kI85Vzzo_qrI2V-zDxjoC5I" />

  <%- include("./partials/head") %>

  <style>
    body {
      background-color: #1e1e2f;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      color: #f1f1f1;
    }

    h1,
    h3 {
      color: #ffffff;
    }

    .section-header {
      padding-top: 60px;
      padding-bottom: 20px;
      text-align: center;
    }

    .card {
      border: none;
      border-radius: 16px;
      overflow: hidden;
      background-color: #2c2c3e;
      color: #ffffff;
      transition: all 0.3s ease-in-out;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(0, 0, 0, 0.35);
    }

    .card-img-top {
      height: 200px;
      object-fit: cover;
    }

    .card-title {
      font-size: 1.2rem;
      font-weight: 600;
    }

    .btn-primary {
      background-color: #6366f1;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #4f46e5;
    }

    .text-muted {
      color: #c0c0c0 !important;
    }

    .no-blog-msg {
      color: #c0c0c0;
      font-size: 1.1rem;
    }
  </style>
</head>

<body>
  <%- include("./partials/nav") %>

  <% if (locals.AllBlog && AllBlog.length > 0) { %>
  <div class="container">
    <div class="section-header">
      <h1>📝 Latest Blogs</h1>
      <p class="text-muted">Fresh stories and insights from our creators</p>
    </div>
    <div class="row g-4 pb-5">
      <% AllBlog.forEach(data => { %>
      <div class="col-sm-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm">
          <img src="<%= data.CoverImageUrl %>" class="card-img-top" alt="Cover Image" />
          <div class="card-body d-flex flex-column">
            <h5 class="card-title"><%= data.Title %></h5>
            <p class="card-text text-truncate">
              <%= data.Body ? data.Body.substring(0, 100) + '...' : '' %>
            </p>
            <a href="/blog/<%= data._id %>" class="btn btn-primary mt-auto">Read More</a>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
  </div>
  <% } else { %>
  <div class="container py-5 text-center">
    <h3 class="no-blog-msg">No blogs found. Be the first to write one! 🚀</h3>
  </div>
  <% } %>

  <%- include("./partials/script") %>
</body>

</html>
